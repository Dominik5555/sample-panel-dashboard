services:
  - type: web
    name: sample-panel-dashboard
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: panel serve app.py --address 0.0.0.0 --port 10000 --allow-websocket-origin=sample-panel-dashboard.onrender.com
